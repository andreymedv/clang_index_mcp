#include <string>

void globalFunction() {
    // Simple global function
}

int computeValue(int x, int y) {
    return x + y;
}

std::string formatMessage(const std::string& msg) {
    return "Message: " + msg;
}
