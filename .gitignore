
!.claude/skills/
!.mcp_cache/.gitkeep
!.test-projects/README.md
!.test-scenarios/*.yaml
!.test-scenarios/README.md
# Benchmark results (contains local paths and model-specific data)
# Custom test scenarios (keep examples, ignore user-created)
# Downloaded libclang binaries
# IDE files
# Local tool state
# MCP cache contents (but keep the folder)
# Mock server optimization runs (ephemeral test output)
# Mock server test results
# Python build artifacts
# Test projects (managed by /test-mcp skill)
# Test results (optional - can be kept for history)
# Virtual environment
*.egg-info/
*.py[cod]
.claude/
.idea/
.kimi/
.vscode/
.mcp_cache/*
clang_index_mcp/.mcp_cache/*
.test-projects/
.test-results/
.test-scenarios/*
__pycache__/
benchmark_results.jsonl
build/
dist/
mcp_env/
clang_index_mcp/libclang/
mock_test_results.json
tests/pytest.log
tools/mock_server/optimization_runs/
