# Test dependencies for Clang Index MCP
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=9.0.0

# Pytest plugins
pytest-cov>=7.0.0          # Coverage reporting
pytest-xdist>=3.8.0        # Parallel test execution
pytest-timeout>=2.4.0      # Timeout protection for tests
pytest-mock>=3.15.0        # Mocking utilities

# Required for running the actual code being tested
-r requirements.txt        # Include main dependencies (mcp, libclang)
